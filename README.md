AutoPR Demo Repository

This repository contains intentionally buggy code for testing the AutoPR Agent.

Example workflows:

- Fix password validation
- Fix discount calculation
- Prevent duplicate user creation
- Generate tests automatically
